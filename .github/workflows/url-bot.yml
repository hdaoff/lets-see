name: URLShort-Bot

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  GitHubMail: "unused-bash@existiert.net"
  GitHubName: "unused-bash"
  # GH_TOKEN >> Github Personal Access Token << In Repository Secrets 

jobs:
  urlbot:
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Set Git Configs & Secrets
        run: |
          git config --global user.email ${GitHubMail}
          git config --global user.name ${GitHubName}
          git config --global color.ui true
          git config --global credential.helper store
          echo "https://${GitHubName}:${{ secrets.GH_TOKEN }}@github.com" > ~/.git-credentials

      #- name: Workspace-Cleanup
      #  uses: unused-bash/slimhub_actions@main

      - name: Install python3 and pip
        run: |
          sudo apt-get -qq -y install python3 python3-pip

      - name: Clone the URLBot-Repo 
        run: |
          git clone https://unused-bash:${{ secrets.GIT_PASS }}@github.com/unused-bash/urlbot urlbot

      - name: Install py3-dependencies
        run: |
          cd urlbot
          pip3 install -r requirements.txt

      - name: Run the Bot
        timeout-minutes: 320
        continue-on-error: true
        run: |
          cd urlbot
          python3 main_bot.py

      - name: Start The Loop
        run: |
          git clone https://unused-bash:${{ secrets.GIT_PASS }}@github.com/${GITHUB_REPOSITORY} loop
          cd loop || exit 1
          echo $(( RANDOM - ( RANDOM % RANDOM ) )) > looper.txt
          git add looper.txt
          git commit -m "Looping at $(date -u +%D-%T%P)"
          git push -q https://github.com/${GITHUB_REPOSITORY} HEAD:main
